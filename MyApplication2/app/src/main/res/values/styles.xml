<resources>
    <!-- Tu tema principal -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- colorPrimary, colorSecondary, etc. -->
    </style>

    <!-- Tema igual pero sin barra de acción -->
    <style name="Theme.MyApplication.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- hereda todo de Theme.MyApplication salvo la ActionBar -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorSecondary">@color/gray_light</item>
        <!-- … otros atributos que necesites heredar -->
    </style>
</resources>